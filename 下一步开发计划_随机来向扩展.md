# 第一阶段开发计划：初始状态随机化（随机来向扩展）

> 依据：`机械臂动态抓取项目截图整理与任务分析.docx`（策划案）第6、7、8节 + `README.md` 第9~11节 + 对 `predictive_grasp_expert.py`（共1930行）的逐行代码分析。
> 目标读者：开发者本人，用于按步骤对照代码修改。

---

## 0. 结论先行

`predictive_grasp_expert.py` 里，**预测/规划层（`choose_intercept_plan` 等）其实已经是任意方向通用的**，不需要改；
**真正写死了 +Y 方向的只有两块**：

1. 环境初始化（目标出生位置、初速度）—— 只在 y 分量赋值。
2. "y 方向专项速度伺服" (`control_y_velocity_only`) —— 只对 `action[1]` 生效，x 轴仍走普通 PD。

策划案第8节里让你先整理的 `y_offset`、`v[...,1]`、`control_y_velocity_only`、`action[1]`，就是精确对应下面第2、3步要改的位置。按下面顺序做，不要跳步。

---

## 1. 第一步：复现固定基线（不改代码）

先确认现状能跑通，作为后续所有修改的对照组（回归测试基准）。

```bash
cd predictive_grasp_expert
set KMP_DUPLICATE_LIB_OK=TRUE
python predictive_grasp_expert.py --render --variant full_sync --seed 0 --steps 500 --debug_every 20 --drift_speed 0.02 --spin_speed 0.05 --y_offset -0.30 --cube_z 0.14 --no-show_markers --log_csv logs/baseline_full_sync.csv --summary_csv results/baseline_summary.csv
```

确认终端最后输出：
```
final_phase=done
grasp_latched=1
```
把这次的 `results/baseline_summary.csv` 留着，作为后面新脚本"0度方向应该和它几乎一样"的对照。

---

## 2. 第二步：复制文件，不改原文件

```bash
cd predictive_grasp_expert
cp predictive_grasp_expert.py predictive_grasp_random_direction.py
```

之后所有改动都在 `predictive_grasp_random_direction.py` 里做。原文件保持不动，方便随时 diff 对照和回退。

---

## 3. 第三步：环境初始化改成任意平面方向（核心改动 A）

### 3.1 现状（第259~289行 `_initialize_episode`）

```python
def _initialize_episode(self, env_idx: torch.Tensor, options: dict):
    ...
    y_offset = get_scalar("y_offset", 0.0)          # 第272行：只有 y 偏移
    drift_speed = get_scalar("drift_speed", 0.0)    # 第273行
    spin_speed = get_scalar("spin_speed", 0.0)
    cube_z = get_scalar("cube_z", self.cube_xyz[2])

    p = torch.zeros((b, 3), device=device)
    p[..., 0] = self.cube_xyz[0]
    p[..., 1] = self.cube_xyz[1] + y_offset          # 第279行：出生点只沿 y 偏移
    p[..., 2] = cube_z
    self.cube.set_pose(...)

    v = torch.zeros((b, 3), device=device)
    v[..., 1] = drift_speed                          # 第284行：初速度只有 y 分量
    self.cube.set_linear_velocity(v)
```

### 3.2 要改成什么样

把 `y_offset` 换成 `offset_x` / `offset_y` 两个分量（由主程序按角度算好传进来），把 `drift_speed` 换成 `vx` / `vy` 两个分量：

```python
offset_x = get_scalar("offset_x", 0.0)
offset_y = get_scalar("offset_y", 0.0)
vx = get_scalar("vx", 0.0)
vy = get_scalar("vy", 0.0)
spin_speed = get_scalar("spin_speed", 0.0)
cube_z = get_scalar("cube_z", self.cube_xyz[2])

p = torch.zeros((b, 3), device=device)
p[..., 0] = self.cube_xyz[0] + offset_x
p[..., 1] = self.cube_xyz[1] + offset_y
p[..., 2] = cube_z
self.cube.set_pose(...)

v = torch.zeros((b, 3), device=device)
v[..., 0] = vx
v[..., 1] = vy
self.cube.set_linear_velocity(v)
```

环境类本身不需要知道"角度"这个概念，它只认 `offset_x/offset_y/vx/vy`——角度到分量的转换放在 `main()` 里做（下一步）。

### 3.3 `main()` 里怎么算 `offset_x/offset_y/vx/vy`（对应策划案"不要独立随机初始位置和方向，要先定抓取区域中心再反推初始位置"）

在 `main()` 里、`gym.make(...)` / `env.reset(...)` 之前（约第1107~1132行附近），新增：

```python
theta = math.radians(args.motion_angle_deg)
vx = args.drift_speed * math.cos(theta)
vy = args.drift_speed * math.sin(theta)

# 抓取区域中心：用当前 cube_xyz 的 xy 作为区域中心（和现在 y_offset=0 时的落点一致）
grasp_region_center_xy = np.array([0.15, 0.0], dtype=np.float64)  # 对应 DriftPickCube... 的 cube_xyz 默认值

# 反推：让目标在 args.approach_lead_time 秒后，正好运动到区域中心
offset_x = -vx * args.approach_lead_time
offset_y = -vy * args.approach_lead_time
```

然后把 `env.reset(options=dict(...))` 里的 `y_offset=args.y_offset, drift_speed=args.drift_speed` 换成：
```python
options=dict(
    offset_x=offset_x,
    offset_y=offset_y,
    vx=vx,
    vy=vy,
    spin_speed=args.spin_speed,
    cube_z=args.cube_z,
),
```

### 3.4 新增 / 替换的命令行参数

| 位置(原行号) | 原参数 | 改动 |
|---|---|---|
| 969 `--drift_speed` | 保留，含义变为"平面运动速度大小" |
| 970 `--y_offset` | **删除**，改为下面两个新参数 |
| 新增 | `--motion_angle_deg` | `float`，默认 `90.0`（90°=原来的 +Y，方便和旧基线对照） |
| 新增 | `--approach_lead_time` | `float`，默认参考旧 `abs(y_offset/drift_speed)`，例如 `-0.30/0.02=15`，可先设默认 `15.0`，后续再调 |

> 建议先跑一次 `--motion_angle_deg 90` + 相同 `drift_speed/spin_speed/cube_z`，理论上应该复现第1步的基线结果（因为 90° 就是原来的 +Y 方向），这是验证第3步改动是否正确的最直接手段。

---

## 4. 第四步：把"y 方向专项速度伺服"推广到任意方向（核心改动 B，最容易出错的地方）

### 4.1 为什么不能简单地"分别控制 x、y 速度"

第1049~1072行的增益参数（`launch_kp_x_scale=0.60` vs `launch_kp_y_scale=1.35`，`descend_kp_x_scale=0.75` vs `descend_kp_y_scale=1.55` 等等）**不是 x/y 对称的**——因为原来的运动方向就是 +Y，所以"沿运动方向"(y) 用的是更激进/带积分伺服的增益，"垂直运动方向"(x) 用的是普通 PD。

如果只是简单地把 `control_y_velocity_only` 复制一份用在 x 上（各自独立控制 x、y 速度），当目标沿 45° 方向运动时，你会把"沿运动方向该有的激进增益"和"垂直方向该有的保守增益"按固定的世界系 x/y 分配错，导致 45°/135° 等方向抓取效果明显变差、且四个象限表现不一致。

**推荐做法（对应策划案"或者在沿目标运动方向/垂直运动方向坐标系下设计控制"）：** 把控制律从"世界系 x/y"改到"沿动/垂直动坐标系"，原来 y 轴的增益给"沿动方向"分量用，原来 x 轴的增益给"垂直方向"分量用，算完再转回世界系写入 `action[0]/action[1]`。这样代码结构改动小，且完全复用已经调好的增益，不需要重新调参。

### 4.2 具体改法

**(1) 增加两个方向向量的辅助函数**（放在第448行 `make_planar_axes` 附近即可）：

```python
def motion_frame_axes(vx: float, vy: float):
    """返回 (沿运动方向单位向量, 垂直运动方向单位向量)。速度为0时退化为世界 Y/X。"""
    v = np.array([vx, vy], dtype=np.float64)
    speed = np.linalg.norm(v)
    if speed < 1e-8:
        return np.array([0.0, 1.0]), np.array([1.0, 0.0])
    along = v / speed
    perp = np.array([-along[1], along[0]])
    return along, perp
```

**(2) 改造 `control_y_velocity_only`（第851~884行）→ `control_along_velocity_only`**

把入参 `target_vy/cur_vy` 换成 `target_v_along/cur_v_along`（标量，语义不变，只是把"世界 y 速度"换成"沿运动方向的速度"），函数体逻辑完全不用动。

**(3) 改造调用点（第1659~1672行）**

现状：
```python
if args.vy_vel_only and phase in ("launch", "descend", "close"):
    action_y, vy_int, ... = control_y_velocity_only(
        target_vy=float(ref_vel[1]),
        cur_vy=float(ee_v[1]),
        ...
    )
    action[1] = float(action_y)
```

改为：
```python
if args.vy_vel_only and phase in ("launch", "descend", "close"):
    along, perp = motion_frame_axes(plan.target_vel[0], plan.target_vel[1])
    target_v_along = float(np.dot(ref_vel[:2], along))
    cur_v_along = float(np.dot(ee_v[:2], along))
    action_along, vy_int, vy_err_only, vy_dbg_ff, vy_dbg_fb, vy_dbg_i = control_along_velocity_only(
        target_v_along=target_v_along,
        cur_v_along=cur_v_along,
        dt=float(dt),
        vy_int=float(vy_int),
        ee_v_per_action_y=float(args.ee_v_per_action_y),
        kv_action=float(args.vy_kv_action),
        ki_action=float(args.vy_ki_action),
        int_clip=float(args.vy_int_clip),
        action_clip=float(args.vy_action_clip),
    )
    # 沿动方向分量用专项伺服结果替换；垂直分量保留 control_xyz() 已经算好的 u_xyz 结果
    action_perp = float(np.dot(action[:2].astype(np.float64), perp))
    action[:2] = (action_along * along + action_perp * perp).astype(np.float32)
    vy_only_active = True
```

**(4) 改造增益选择（第1570~1601行 launch/descend 分支）**

现状把 `launch_kp_x_scale` 固定给 `kp_xyz[0]`（世界x）、`launch_kp_y_scale` 固定给 `kp_xyz[1]`（世界y）。要改成：先在"沿动/垂直动"坐标系里用这两个 scale 算出 `kp_along/kp_perp`，再投影回世界 x/y：

```python
along, perp = motion_frame_axes(plan.target_vel[0], plan.target_vel[1])
kp_along = args.kp_xy_sync * args.launch_kp_y_scale   # 沿用原 y-scale（原来沿动方向就是y）
kp_perp  = args.kp_xy_sync * args.launch_kp_x_scale   # 沿用原 x-scale
kp_xy_world = kp_along * np.abs(along) + kp_perp * np.abs(perp)  # 简化投影，见下方说明
```

> 注：更严谨的做法是把 `pos_err`/`vel_err` 本身旋转到 along/perp 坐标系里分别乘增益、算完 u 再转回世界系，而不是直接对增益标量做"投影"（增益是标量，不能直接向量合成）。**建议实际实现时，把 `control_xyz()` 也做一个 along/perp 版本**：即在 `control_xyz` 内部，先把 `pos_err`、`vel_err`、`ref_vel` 通过 `along/perp` 分解成两个标量分别乘 `kp_along/kp_perp`、`kv_along/kv_perp`、`ff_along/ff_perp`，算出两个标量指令后再合成回 x/y 向量。这是本阶段唯一需要"新写一点逻辑"而不是简单替换的地方，建议单独写一个 `control_xy_along_perp()` 函数，专门在 launch/descend/close 三个阶段替换掉现在直接调用 `control_xyz` 处理 xy 分量的部分（z 分量不受影响，仍用原 `control_xyz` 逻辑）。

**(5) `axis_scale` 里 `axis_scale[1] *= args.launch_y_action_scale` 等（第1625~1629行）同理**，也要从"固定改 world y" 改成"沿动方向再投影回 xy"。

这一步工作量最大，建议先在 0°/90°/180°/270°（沿世界坐标轴的四个方向）测试——这四个方向下 along/perp 分解退化成原来的 x/y，如果这四个方向都能正常复现基线效果，说明坐标变换写对了；再测 45°/135° 等斜向。

---

## 5. 第五步：先测 8 个固定方向

`--motion_angle_deg` 依次取 `0 45 90 135 180 225 270 315`，各跑一次并记录到同一个 `summary_csv`：

```bash
for angle in 0 45 90 135 180 225 270 315; do
  python predictive_grasp_random_direction.py --variant full_sync --seed 0 --steps 500 \
    --drift_speed 0.02 --spin_speed 0.05 --motion_angle_deg $angle --cube_z 0.14 \
    --no-show_markers --log_csv logs/dir_${angle}.csv --summary_csv results/dir_summary.csv
done
```
(PowerShell 下用 `foreach ($angle in 0,45,90,135,180,225,270,315) { ... }`)

检查 `results/dir_summary.csv` 里每一行的 `success`、`final_phase`、`max_rel_speed`、`max_abs_yaw_err_deg` 等指标，8 个方向应该都能 `success=1`、`final_phase=done`，且各方向的 `max_rel_speed` 量级接近（不应该出现某几个方向明显更差，那说明第4步的坐标系改造没做对/没做全）。

对 `predictive_intercept`、`linear_sync`、`full_sync` 三种 variant 都各跑一遍 8 方向（策划案第6节要求的消融实验）。

---

## 6. 第六步：加入随机连续方向

8 个固定方向都稳定后，新增：

```python
ap.add_argument("--random_direction", action=argparse.BooleanOptionalAction, default=False)
```

在 `motion_angle_deg` 计算前，若 `--random_direction`，用 `--seed` 派生的 `np.random.default_rng(args.seed)` 采样 `motion_angle_deg = rng.uniform(0, 360)`，其余参数（线速度大小、角速度大小、`cube_z`、方块尺寸等）按策划案要求**第一轮先保持固定**，只随机来向。

务必把采样到的角度打进 `summary_csv`（新增一列 `motion_angle_deg`），保证同一个 `--seed` 下角度可复现、可追溯。

---

## 7. 交付清单（对照策划案第11节）

- [ ] `predictive_grasp_random_direction.py`（本次修改后的程序）
- [ ] 8 个固定方向 × 3 个 variant 的测试结果 CSV（`results/dir_summary.csv`）
- [ ] 每个工况的方向角、随机种子、成功与否的汇总表（可直接由 `dir_summary.csv` 整理）
- [ ] 一份"原程序里哪些部分依赖固定 +Y"的说明——**本文档第0节和第3~4节已经写好，可直接引用**
- [ ] 连续随机方向的初步测试结果（若干个随机种子，记录角度分布和成功率）

---

## 8. 风险提示 / 常见坑

1. **第4步的增益坐标变换如果只改一半**（比如只改了速度伺服、没改 `axis_scale`/`kp_xy_sync` 的方向性缩放），会出现"部分角度能抓、部分角度总是慢半拍脱手"的现象，排查时优先看 `log_csv` 里 `rel_vx/rel_vy`（相对速度）在接触时刻是否明显偏大。
2. **`--motion_angle_deg 90` 必须能复现第1步基线**，这是判断第3步环境初始化改动是否正确的硬指标，务必先验证这一步再往下走。
3. `choose_intercept_plan`、`linear_profile_along_track`、`linear_backtrack_distance`（第590~773行）**不需要改**，它们已经是基于速度大小和方向的通用实现，不要在这几个函数里做多余修改。
4. 角速度（`spin_speed`，绕世界 z 轴）与平面运动方向无关，`choose_face_target_yaw` 等偏航相关逻辑不需要因为方向改变而修改。
